import React from "react";
import ReactDOM from "react-dom/client";
import Shop from "./pages/Shop.jsx";   // <- your component

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Shop />
  </React.StrictMode>
);

